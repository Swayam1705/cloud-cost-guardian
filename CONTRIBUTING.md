# Contributing

See [docs/contribution-guide.md](docs/contribution-guide.md) for the full guide.

Short version: fork → branch → `.\scripts\setup_local.ps1 -Dev` → change + tests →
`.\scripts\validate.ps1` green → PR with a clear *why*. No paid services, no unsafe deletes,
RDS stays recommendation-only.
