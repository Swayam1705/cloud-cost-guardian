# Runtime dependencies (installed via `pip install -r requirements.txt`).
# Also installs the project itself in editable mode so `python -m cloud_cost_guardian...` works.
boto3>=1.34,<2
pydantic>=2.5,<3
pydantic-settings>=2.1,<3
-e .
