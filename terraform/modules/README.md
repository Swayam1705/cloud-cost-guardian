# terraform/modules

Reserved for reusable modules if this deployment grows (e.g. a `scanner` module that can be
instantiated per region). The current footprint is small enough that flat root files are clearer.
